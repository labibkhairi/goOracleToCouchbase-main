package server

func (server *Server) initializeRoute() {
	// server.Router.HandleFunc("/", controller.Hello).Methods("GET")
	// server.Router.HandleFunc("/awb/{awb}", controller.Awb).Methods("POST")
}
